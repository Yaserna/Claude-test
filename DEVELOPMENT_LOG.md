# گزارش توسعه — InfinityClone (شاخه‌ی Cloner)

این فایل خلاصه‌ی کامل مسیر ساخت اپ، تصمیم‌ها، باگ‌ها و رفع‌ها، وضعیت فعلی و
قدم‌های بعدی است تا کل سابقه‌ی کار در همین مخزن بماند.

## هدف
ساخت یک اپ اندروید برای **کلون کردن نامحدود** اپ‌ها (اجرای چند نسخه‌ی مستقل از یک
اپ)، بدون روت، روی اندروید ۱۲. کاربر روش **مجازی‌سازی** را ترجیح داد.

## تصمیم معماری
- نوشتن موتور مجازی‌سازی از صفر عملی نبود؛ روی موتور متن‌باز **NewBlackbox**
  (فورک BlackBox) سوار شدیم و به‌صورت ماژول داخل مخزن آوردیم.
- ماژول‌ها: `app` (اپ ما)، `Bcore` (هسته‌ی موتور + بخش C/C++)، `black-reflection`،
  `compiler`.
- کل اپ فقط با اینترفیس `CloneEngine` کار می‌کند؛ پیاده‌سازی واقعی `BlackBoxEngine`.
- چون اینجا Android SDK نیست، build با **GitHub Actions** انجام می‌شود
  (`.github/workflows/android.yml`، نیازمند NDK 29.0.13846066 و JDK 21).

## مسیر باگ‌ها و رفع‌ها (به‌ترتیب)
1. **فریز کامل هنگام کلون** → عملیات سنگین موتور روی نخ اصلی بود؛ منتقل شد به
   `Dispatchers.IO` + نشانگر پیشرفت.
2. **گیرکردن نصب روی MIUI** → ابتدا `daemon` خاموش شد و موتور قبل از استفاده
   «گرم» شد (`ensureBlackProcessInitialized`). با لاگ ADB مشخص شد موتور سالم بالا
   می‌آید.
3. **علت اصلی گیرکردن** (از روی لاگ ADB): تابع `nextFreeUserId` تابع `isInstalled`
   را در حلقه صدا می‌زد و روی این دستگاه پاسخ «جایگزین» باعث **حلقه‌ی بی‌نهایت**
   می‌شد. جایگزین شد با پیمایش فهرست فضاهای موجود. → **کلون کار کرد.**
4. **ANR پنجره‌ی خطا** → خواندن `logcat` روی نخ اصلی؛ کلاً حذف شد.

## ویژگی‌های اضافه‌شده
- **امضای ثابت**: کلید دیباگ ثابت در `keystore/debug.keystore` تا آپدیت‌ها بدون
  حذف نصب شوند.
- **تغییر نام کلون** (ذخیره در SharedPreferences) + **آیکن در صفحه‌ی اصلی**
  (pinned shortcut via `ShortcutActivity`) + نمایش آیکن واقعی اپ‌ها.

## تغییرات در موتور برای اثر انگشت/بیومتریک
- موتور فقط `FingerprintManager` قدیمی را هوک کرده بود و آن هم باگ داشت
  (`getWho()` اینترفیس اشتباه `IGraphicsStats` را برمی‌گرداند → هوک‌ها اعمال
  نمی‌شدند).
- اضافه شد: mirrorهای درست `IFingerprintService` و `IAuthService`؛ رفع باگ
  `IFingerprintManagerProxy`؛ پروکسی جدید `IAuthServiceProxy` برای
  `BiometricPrompt` مدرن (سرویس `auth`، API 29+) با جایگزینی نام پکیج میزبان؛ ثبت
  در `HookManager`.

## وضعیت فعلی
- ✅ کلون‌سازی نامحدود کار می‌کند (تست‌شده روی Redmi Note 9 Pro / MIUI / اندروید ۱۲).
- ✅ آپدیت بدون حذف، تغییر نام، آیکن صفحه‌ی اصلی.
- 🧪 بیومتریک: کد اضافه شد، **در انتظار تست کاربر**.

## محدودیت‌های شناخته‌شده
- روی MIUI باید **Autostart** و **باتری بدون محدودیت** برای اپ فعال باشد.
- اپ‌های بانکی/حساس به‌خاطر Play Integrity ممکن است داخل فضای مجازی اجرا نشوند.
- بیومتریک با کلید رمزنگاری گره‌خورده (CryptoObject/keystore) ممکن است کامل کار نکند.

## قدم‌های بعدی
- [ ] تست بیومتریک روی دستگاه؛ در صورت مشکل، گرفتن لاگ ADB با فیلتر
      `Biometric|Fingerprint|AuthService|keystore`.
- [ ] بهبود UI (صفحه‌ی تنظیمات، مدیریت بهتر فضاها).

## نحوه‌ی ساخت
هر push روی این شاخه، workflow «Build APK» را اجرا می‌کند؛ خروجی APK در
بخش Artifacts همان run قابل دانلود است.
