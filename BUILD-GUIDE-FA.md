# راهنمای کامل ساخت اپ روی ویندوز

این راهنما فرض می‌کند تخصص برنامه‌نویسی نداری. قدم‌به‌قدم پیش برو.

---

## بخش ۱: نصب پیش‌نیازها (یک‌بار)

### ۱) Git
- دانلود: https://git-scm.com/download/win
- فایل را اجرا کن و همه‌جا **Next** بزن، آخرش **Install**.

### ۲) Python
- دانلود: https://www.python.org/downloads/
- فایل را اجرا کن. **مهم:** در اولین صفحه، تیک **«Add Python to PATH»** را بزن، بعد **Install Now**.

### ۳) Android Studio
- دانلود: https://developer.android.com/studio
- نصب کن و در Setup Wizard گزینه‌ی **Standard** را انتخاب کن تا SDK و ابزارهای پایه نصب شوند.
- بعد از نصب: **More Actions → SDK Manager → تب SDK Tools** و این دو را تیک بزن و نصب کن:
  - ☑️ **NDK (Side by side)**
  - ☑️ **CMake**

### نیازمندی‌ها
- حداقل ۲۰ تا ۳۰ گیگابایت فضای خالی و ۸ گیگ رم.

---

## بخش ۲: دریافت پروژه و اعمال تغییرات

1. این مخزن را دانلود کن: در صفحه‌ی گیت‌هاب، دکمه‌ی سبز **Code → Download ZIP** را بزن و جایی (مثلاً `D:\TelegramApp`) از حالت فشرده خارج کن.
2. وارد همان پوشه شو، در نوار آدرس **File Explorer** بنویس `cmd` و Enter بزن تا Command Prompt در آن پوشه باز شود.
3. دستور زیر را اجرا کن:
   ```
   setup.bat
   ```
4. این اسکریپت سورس تلگرام را دانلود می‌کند (حدود ۱ گیگ، کمی صبر کن) و تغییرات اختصاصی را اعمال می‌کند. در پایان پیام **«تمام شد!»** را می‌بینی و یک پوشه‌ی جدید به اسم `Telegram` ساخته می‌شود.

---

## بخش ۳: گرفتن کلید API تلگرام (یک‌بار)

اپ برای وصل‌شدن به سرور تلگرام به کلید نیاز دارد:
1. برو به https://my.telegram.org و با شماره موبایلت وارد شو.
2. روی **API development tools** بزن، یک اپ بساز (هر نام و توضیحی).
3. دو مقدار **api_id** و **api_hash** به تو داده می‌شود. این‌ها را نگه دار.
4. در فایل `Telegram/TMessagesProj/src/main/java/org/telegram/messenger/BuildVars.java` این دو مقدار را جایگزین کن:
   ```java
   public static int APP_ID = اینجا_api_id;
   public static String APP_HASH = "اینجا api_hash";
   ```
   (محل دقیق را در همان فایل با جستجوی `APP_ID` پیدا می‌کنی.)

---

## بخش ۴: باز کردن در Android Studio و Build

1. Android Studio را باز کن → **Open** → پوشه‌ی `Telegram` را انتخاب کن.
2. کمی صبر کن تا Gradle همگام‌سازی (Sync) شود (بار اول طول می‌کشد).
3. از منوی بالا: **Build → Build Bundle(s) / APK(s) → Build APK(s)**.
4. وقتی تمام شد، روی لینک **locate** کلیک کن تا فایل `app-debug.apk` را پیدا کنی.
5. این فایل APK را روی گوشی اندرویدی نصب کن (نصب از منابع نامطمئن را فعال کن).

---

## بخش ۵: تغییر نام و آیکون اپ (قبل از انتشار)

طبق قوانین تلگرام، نباید اپ را «Telegram» صدا بزنی:
- نام اپ: در `build_config.json` مقدار `app_name` را تنظیم کن (در نسخه‌های بعدی به‌صورت خودکار اعمال می‌شود).
- فعلاً نام را دستی می‌توان در `Telegram/TMessagesProj/src/main/res/values/strings.xml` (کلید `AppName`) تغییر داد.
- آیکون: فایل‌های `ic_launcher` در پوشه‌های `res/mipmap-*` را با آیکون خودت جایگزین کن.

> اگر در هر مرحله گیر کردی، پیام بده تا کمکت کنم.
